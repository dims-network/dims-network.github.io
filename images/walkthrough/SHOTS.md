# Screenshots for the walkthrough

Drop the files here under exactly these names and
[`tutorial.html`](../../tutorial.html) picks them up — until then each one
renders as a dashed panel saying what belongs in it, so the page is readable
with none of them taken.

**How to take them.** Browser window **1280 × 800**, no bookmarks bar, default
zoom, light theme. Capture the wizard's own panel rather than the whole screen
where you can; include the step bar across the top on the wizard shots, because
it is how a reader keeps their place. Save as PNG. Use the example study
(ConvoConnect-Mini) so what is on screen matches what the page describes — real
data in a screenshot on a public page is a data-visibility decision, not a
convenience.

| File | What should be on screen |
|---|---|
| `01-wizard-opens.png` | The wizard as it first opens: step 1 selected, the seven step buttons across the top, nothing filled in yet. |
| `02-your-study.png` | Step 1 filled in: a folder path, Private selected, title 'ConvoConnect-Mini'. Include the visibility explanation under the radio buttons. |
| `03-sessions-files.png` | The file list after Load the example study: 16 rows grouped under dyad01 and dyad02, each row showing its role and data type. |
| `04-align.png` | Step 3 showing dyad02: the video track visibly longer than the measurement tracks, with the dual-handle trim slider. |
| `05-analyses.png` | Step 4 with only ELAN and recurrence switched on, and rtpjSync the single selected chip under recurrence. |
| `07-build.png` | Step 5 after a successful build: the confirmation and the list of files placed. |
| `08-compute.png` | Step 6 mid-run: the live progress log, and the elapsed-time line underneath it. |
| `09-open-it.png` | Step 7 with the Open the dashboard button and the deployment commands underneath. |
| `10-dashboard-timeseries.png` | The dashboard's main view for dyad01: the traces on the left, video and transcript on the right, playhead parked on the peak at 1:16. |
| `10b-dashboard-elan.png` | The ELAN Annotations tab for dyad01, its phases tier laid along the same timeline as the measurements. |
| `opt-rqa-tab.png` | The recurrence tab for dyad01 rtpjSync — the plot plus the metrics panel beside it. |
| `opt-crqa-tab.png` | The Cross-RQA tab for dyad01, the two partners' right hands, with the structure sitting off the diagonal. |
| `opt-cw-step4.png` | Step 4 with cross-wavelet switched on and two hand pairs selected in its chip row. |
| `opt-cw-tab.png` | The cross-wavelet tab for dyad01, both hand-speed pairs, with the significance contours and the white phase arrows visible. Include the (i) beside the heading. |
| `opt-network-step4.png` | The network diagram with two figures (Left partner, Right partner), each partner's two hands placed on them, and the two lines drawn between them. |
| `opt-network-tab.png` | The network tab for dyad01 with the playhead on a synchrony peak, so the caption reads a window rather than the whole recording. Include the controls row — **Edges show** and **Solid above** are the two a reader needs to see — and the legend under the figure. |

Once a shot exists, check the caption under it in `tutorial.html` still
describes what you actually captured — several captions name specific times and
values from the example study.
